# R5: UX Inference Audit — Run 2026-06-10

## Critical Findings

### 1. `alert()` blocked on iOS PWAs (HIGH)
**File:** `src/components/report/TeamCardCTA.tsx:40`
**Issue:** `alert("Couldn't generate the team card...")` silently no-ops in installed PWAs.
**Fix:** Replace with inline error banner / toast.

### 2. Standardize API error handling (HIGH)
80+ `console.error` instances scattered across API routes with no user-facing feedback.
**Fix:** Create ErrorFormatter utility; wrap routes.

### 3. Missing loading.tsx skeletons (MEDIUM)
- `/champions` — data-heavy, blank flash on slow networks
- `/compare` — slow network = blank page
- `/creator/[name]` — profile data fetch blocks render
- MatchTracker — no skeleton on mount
- CollaboratorPanel — no loading state on open

### 4. Public-share validation friction (MEDIUM)
Validation in ShareModal hides "why can't I publish" until toggle click.
**Fix:** Inline "Needed to publish" badges on Creator/Tag fields.

### 5. Sample team path not discoverable on home (MEDIUM)
Walkthrough doesn't mention sample teams; new users land on blank builder.
**Fix:** Add "Try a sample team" CTA in home hero.

## Top empty states needing love
1. MatchTracker — "No matches logged yet" with no CTA
2. CompareContent — "No reports yet" in dropdown with no context
3. VersionHistoryPanel — "No versions yet" no guidance
4. CommentSection — "No comments yet. Be the first!" no hook
5. CollaboratorPanel — "No collaborators yet" no invite hint

## Clipboard silent-fail (MEDIUM)
ShareModal copy-to-clipboard `catch { /* no-op */ }` on lines 193-204 — on non-HTTPS dev, user sees "Copied" but pastes nothing. Should toast "Clipboard unavailable — text is selectable above."

## Conflict-Risk Check
- TeamCardCTA.tsx — no conflict
- API routes — MEDIUM conflict risk (heavy churn)
- Loading skeletons — no conflict
- ShareModal — no conflict
