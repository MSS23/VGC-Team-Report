# C2 — TypeScript Strictness (2026-07-06)

## Top 8 quick wins (single-line, no runtime change)

1. **`src/lib/db.ts:4`** — replace `process.env.DATABASE_URL!` with a fail-fast guard (`if (!url) throw new Error(...)`) for readable startup failure.

2. **`src/app/api/discord/route.ts:33`** — annotate `linearQuery` return as `Promise<{ data?: unknown; errors?: unknown }>` instead of inferred `Promise<any>`. 8 downstream call sites currently traverse `result.data?.issue.state.name` with zero type help.

3. **`src/hooks/useCollaborativeSync.ts:98`** — replace untyped destructure with `const parsed: unknown = JSON.parse(e.data); const { version, state } = parsed as { version: number; state: ShareableState };`. This is the 98k-versions-loop code path — worth a schema.

4. **`src/components/report/MatchupPlanSlide.tsx:277`** — hoist `if (!mon.calculatedStats) return null;` above `.map` and drop the `!`. Removes crash vector when stat calc fails.

5. **`src/components/report/TeamStats.tsx:32`** — change `.filter((p) => p.data?.baseStats)` predicate to type-predicate form. Eliminates the `p.data!` bang and gives `.map` a narrowed type.

6. **`src/lib/i18n/index.ts:96`** — add `: I18nContextValue` return type on `useTranslation()`. Pins public contract.

7. **`src/hooks/useSlideSystem.ts:56` + `src/components/report/CommonModesSlide.tsx:108`** — add `commonModesTitle` to `TranslationKeys` and drop the `t as unknown as Record<...>` double-casts. Fixes two callers at once.

8. **`src/app/api/webhooks/clerk/route.ts:46`** — remove `as unknown as ClerkUserCreatedData`; narrow via discriminated union from Clerk SDK (`if (event.type === "user.created") { const data = event.data; ... }`).

## Wave 2 candidate: MEDIUM confidence — items #1, #2, #6 are the fastest (no cross-file changes).
