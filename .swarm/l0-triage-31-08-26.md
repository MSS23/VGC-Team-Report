# L0 Linear Triage — 2026-08-31

Board snapshot (team VGC Team Report, states Backlog / Todo / In Progress): **75 open issues**.
"In Review" deliberately excluded from the pull — that work is implemented and waiting on merges.
No open issue in these states carries the **Bug** label (the label exists on the team; zero open
Backlog/Todo/In Progress issues use it). Nothing has been filed since 2026-08-10.

```json
[
  {
    "identifier": "VGC-207",
    "title": "Anonymous quick-share: paste-to-URL without login",
    "url": "https://linear.app/vgc-team-report/issue/VGC-207/anonymous-quick-share-paste-to-url-without-login",
    "files_likely_to_change": [
      "src/hooks/useShareFlow.ts",
      "src/hooks/useShareUrl.ts",
      "src/lib/sharing/url-codec.ts",
      "src/components/ui/ShareModal.tsx",
      "src/hooks/useHomePage.ts",
      "src/lib/sharing/__tests__/"
    ],
    "scope_estimate": "~4h",
    "priority": "P2 High (Todo, filed 2026-05-18)",
    "why_implementable": "Pure client-side path exists already and needs NO database. src/hooks/useShareUrl.ts:56 and useHomePage.ts:66 still DECODE a legacy '#data=' inline-data share link, but nothing generates one any more, so guests get 'Sign in to save and share your team report.' (useShareFlow.ts:98) and a dead end. The encoder in src/lib/sharing/url-codec.ts is intact. Scope this to: signed-out share click produces an inline '#data=' URL (no server write, no TTL, no shares row) plus the post-share auth upsell. Explicitly do NOT touch src/app/api/share/route.ts — its 'saving is an authenticated action, full stop' 401 at line 114-127 is a deliberate security decision and the ephemeral-30-day-TTL half of the ticket needs a shares-table column nobody can migrate tonight. File the TTL/claim flow as a follow-up.",
    "verified_still_broken": true
  },
  {
    "identifier": "VGC-158",
    "title": "Decompose PokemonDetailSlide — 962-line component into focused sub-components",
    "url": "https://linear.app/vgc-team-report/issue/VGC-158/decompose-pokemondetailslide-962-line-component-into-focused-sub",
    "files_likely_to_change": [
      "src/components/report/PokemonDetailSlide.tsx",
      "src/components/report/MegaFormToggle.tsx (new)",
      "src/components/report/StatsPanel.tsx (new)",
      "src/components/report/DetailPanel.tsx (new)",
      "src/components/report/PokemonSlideHeader.tsx (new)"
    ],
    "scope_estimate": "~3-4h",
    "priority": "P3 Medium (Todo, filed 2026-05-10)",
    "why_implementable": "Purely local refactor — no network, no credentials, no DB, no schema. The file is now 974 lines (grown since the ticket was written) and none of the four proposed sub-components exist in src/components/report/. Verified by directory listing: only PokemonDetailSlide.tsx carries all of the slide header, stat panel, mega toggle and detail panel. Fully verifiable by the standard gate (tsc + vitest + next build) because the refactor is behaviour-preserving. Conflict risk: any other agent touching report components — claim src/components/report/ for the night.",
    "verified_still_broken": true
  },
  {
    "identifier": "VGC-248",
    "title": "Security: clear remaining moderate npm vulns (scoped to the @opentelemetry chain)",
    "url": "https://linear.app/vgc-team-report/issue/VGC-248/security-clear-remaining-12-moderate-npm-vulns-breaking-dep-upgrades",
    "files_likely_to_change": [
      "package.json",
      "package-lock.json",
      "src/instrumentation.ts",
      "src/app/api/views/[shareId]/route.ts"
    ],
    "scope_estimate": "~1-2h",
    "priority": "P3 Medium (Backlog, filed 2026-06-29)",
    "why_implementable": "registry.npmjs.org IS reachable from this container — `npm audit` ran successfully. It reports 8 moderate (not 12), 0 high, 0 critical, and every one is the same @opentelemetry/core 2.6.1 W3C-Baggage advisory reached through three DIRECT dependencies: @opentelemetry/exporter-logs-otlp-http, @opentelemetry/sdk-logs (both ^0.214.0 → fixed in 0.221.0) and @opentelemetry/resources. Blast radius is two files (src/instrumentation.ts, src/app/api/views/[shareId]/route.ts), so an API break surfaces immediately at tsc/build. IMPORTANT — three of the ticket's four checkboxes are STALE and must be skipped: there is no @sentry/nextjs dependency any more; `next` is already ^16.3.0 (past the postcss advisory); and the CORS preview-origin 'stretch' item was already done — src/lib/security/cors.ts:35 is anchored on the `-mss23s-projects` scope suffix and Allow-Credentials was removed under VGC-274. Update the ticket rather than re-fixing those.",
    "verified_still_broken": true
  }
]
```

Only three. That is the honest read of this board, and it is the same signal VGC-265 raises:
what is left in Backlog/Todo is overwhelmingly blocked on things this container structurally
cannot do, not on implementation capacity.

## REJECTED AND WHY

**1. `no-claude` label and not a Bug — skipped entirely (22):** VGC-263, 252, 250, 249, 244, 241,
240, 239, 238, 136, 60, 6, 16, 7, 63, 57, 58, 59, 184, 56, 62, 21. Two of these read like defects
rather than feature asks, so I checked them in code before rejecting — **both are already fixed**:
VGC-263 (Levitate should grant Ground immunity) is implemented in `src/lib/data/type-chart.ts` with
a passing test, and the ability is passed at both call sites (`DefensiveCoverageChart.tsx:48`,
`CompareContent.tsx:191`); VGC-249's "mega speed tiers button has no effect" is handled by the
`megaStates` / `showMegaTiers` path in `SpeedTierChart.tsx:163-253`. Relabelling them Bug would buy
nothing — they should be closed. VGC-249's other half (stat-bar colour coding) is a design ask and
`StatColorNote.tsx` already exists.

**2. Needs network egress or real-world data — structurally impossible here (9):** VGC-40 (import
from Limitless VGC), VGC-66 (build top-8 reports after a Regional), VGC-113 (tournament weekend
blitz), VGC-88 (Reddit ads), VGC-81 and VGC-45 (Showdown/community usage stats), VGC-51 (replay
analysis), VGC-187 (capture PWA screenshots of the live site), VGC-227 (blog route + Reg M-A meta
post). **VGC-227 is the single best carve-out on the board** — there is no `/blog` route at all, and
the route half (markdown source, Article JSON-LD following the existing JsonLd pattern, `sitemap.ts`
wiring, ~2-3h) is entirely local. It is rejected only because the post the ticket demands needs real
usage percentages, tournament sample sizes and damage calcs per
`.swarm/drafts/r7-content-brief-champions-meta-2026.md`; with egress blocked an agent could only
fabricate them, and the May-2026 dating has lost its freshness value anyway. File "blog route
infrastructure, no content" as its own ticket and it ships in one night.

**3. Needs database access or credentials the container lacks (10):** VGC-215 (run
vgc195-db-repair.sql against production), VGC-217 (per-species spread pages — SQL aggregation over
`shares.species[]`), VGC-226 and VGC-84 (rental code — needs `shares.rental_code TEXT NULL`),
VGC-200 (notification prefs — needs a user_preferences table), VGC-153 (tiered publishing — needs a
visibility column), VGC-253 (creator_profiles user_id column), VGC-49 (meta snapshot from public
reports), VGC-129 (push notifications — VAPID keys), VGC-5 (Stripe). No Neon credentials, and
shipping UI against a column that does not exist would break the editor.

**4. Needs an action in an external dashboard (6):** VGC-236 (confirm the env-var name in Vercel
before dropping the `LINEAR_WEBHOOK_SECRET` fallback — dropping it blind could break a working
webhook), VGC-222 and VGC-213 (re-enable the webhook in Linear settings; CLAUDE.md also marks this
one stale), VGC-220 (populate POSTHOG_API_KEY / DISCORD_WEBHOOK_URL in the container), VGC-255 (the
egress policy itself — the meta-blocker behind category 2), VGC-72 (Discord bot — needs a bot token
and Discord API egress).

**5. Marketing / outreach / publishing — draft-only by policy (7):** VGC-67, 71, 85 (DM creators,
contact Victory Road, partner with YouTubers), VGC-90 (tutorial blog post with screenshots),
VGC-107 (Product Hunt launch), VGC-148 and VGC-147 (get listed on Victory Road / VGCpedia / Smogon —
duplicates of each other; worth merging).

**6. Epic / multi-day / scope over ~4h (15):** VGC-120, 122, 123, 124 (i18n scaffold + three
translation passes — note `src/lib/i18n/translations/` already ships en/es/fr/it/ja/ko/zh under a
custom lib, so these tickets need re-scoping against reality), VGC-131 (RTL — and there is no RTL
locale in the set, so it cannot be exercised), VGC-41 (full Champions support), VGC-38, 79, 50, 43
(damage calc, EV builder, coverage matrix), VGC-48 (real-time collaboration), VGC-55 (creator
articles), VGC-82 (team builder wizard), VGC-8 (Capacitor app), **VGC-162** (root page.tsx Server
Component refactor — `src/app/page.tsx` is now 1833 lines, not the ~1200 the ticket assumes; the
ticket itself says "significant refactor, feature branch, thorough testing", which is exactly the
class this filter excludes. Recent commits 6cec919/415a281 already took the cheap dynamic-import
wins).

**7. Process/meta tickets with no code deliverable (2):** VGC-164 (permanent session ledger),
VGC-265 (the ~30 unmerged `swarm-nightly-*` branches — confirmed: 35 remote branches, 24 of them
`swarm-nightly-*`. Its concrete example is however now fixed on main: commit 1db8419 "VGC-266:
correct SP definition in llms.txt and FAQ". The remedy is a human merging branches).

**8. Stale — premise no longer true (1):** VGC-221 (Clerk major bump to clear js-cookie advisories).
`@clerk/nextjs` is already `^7.3.2`, not 4.x, and `npm audit` reports 0 high-severity advisories.
Close it.
