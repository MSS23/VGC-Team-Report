# C1 Dead-Code Scan — 2026-07-13

Read-only scan of `/home/user/VGC-Team-Report/src`. Top-10 highest-confidence
findings, ranked. Each has been cross-checked with grep across `src/`,
`.planning/`, and `.swarm/` — matches in markdown/`.planning/` docs do not
count as usages.

Excluded: dynamic-import candidates already confirmed (`export-report`,
`__validate-mega-coverage`, translations), test-only usages (bumped to
medium/low confidence), and re-exports.

---

## 1. Unused exports

### 1a. `exportAsPdf` — HIGH
- **File**: `src/lib/utils/export-report.ts:95-115`
- Only `exportAsImage` is imported (dynamic import in `src/app/page.tsx:488`).
  `exportAsPdf` has zero call sites in `src/`. Removing it also lets you drop
  the local `getJsPDF()` helper (`src/lib/utils/export-report.ts:3-7`) and the
  `jspdf` dependency if it isn't used elsewhere.
- Justification: grep for `exportAsPdf` outside its own file only hits i18n
  translation strings — no code imports it.

### 1b. `asPokemonTypes` — HIGH
- **File**: `src/lib/data/dex-subset.ts:123-125`
- 3-line typed-cast helper. Zero imports anywhere in `src/`.
- Justification: `grep -r asPokemonTypes` matches only the definition line.

### 1c. `getRegMBMegas` — HIGH
- **File**: `src/lib/data/mega-pokemon.ts:846-848`
- Exported function, never imported. The MB-only cohort is consumed via the
  `CHAMPIONS_REG_MB_MEGAS` set (used in `champions-dex.ts`), not this
  helper. Its M-A sibling `getRegMAMegas` IS used; the MB variant is not.
- Justification: no import of `getRegMBMegas` in `src/`.

### 1d. `useGlobalDisplayPrefs` — HIGH
- **File**: `src/lib/hooks/useGlobalDisplayPrefs.ts:36` (whole file, 51 lines)
- Only ever consumed by `DisplayTogglePill` (also dead, see 2a). The single
  external reference is a comment in the file itself. `src/lib/hooks/` is the
  ONLY thing in that directory — the directory can be deleted too.
- Justification: `grep -r useGlobalDisplayPrefs` only hits its own definition.

### 1e. `flushServerEvents` (export-only) — LOW
- **File**: `src/lib/posthog-server.ts:56`
- Used internally by `captureServerEvent` at line 44, but never imported
  externally. The function itself is not dead — only the `export` keyword is
  dead. Trivial cleanup, listed for completeness only.

---

## 2. Orphaned components

### 2a. `DisplayTogglePill` — HIGH
- **File**: `src/components/display/DisplayTogglePill.tsx` (whole file, 267 lines)
- Zero imports in `src/`. The only cross-file mention is a comment in the
  now-dead `useGlobalDisplayPrefs.ts`. This is the ONLY file in
  `src/components/display/` — the directory can be deleted along with it.
- Justification: `grep -r "from.*DisplayTogglePill"` returns nothing.
- Wired dependencies also become dead: `useGlobalDisplayPrefs` (1d) and its
  `.planning/` references.

### 2b. `ConsentGate` — HIGH
- **File**: `src/components/providers/ConsentGate.tsx` (whole file, 37 lines)
- Zero imports in `src/`. Only mentioned in `.planning/phases/08-cookie-consent-*`
  markdown (design docs, not runtime). Analytics gating is done differently
  today — see `PostHogProvider` and `ClarityProvider` which do their own
  consent checks via `hasAnalyticsConsent`.
- Justification: `grep -r "from.*ConsentGate"` inside `src/` returns nothing.

---

## 3. Dead routes

None found with 100% confidence.
- `src/app/notifications/page.tsx` and `src/app/dashboard/notifications/page.tsx`
  look duplicative but are distinct: `/notifications` is the activity feed
  (linked from `NotificationBell`); `/dashboard/notifications` is the email
  preferences page (linked from `DashboardContent`).
- All other `src/app/**/page.tsx` files have at least one internal `href`,
  sitemap entry, or link from a dashboard/nav component.

---

## 4. Dead `.ts` files (whole-file removal)

### 4a. `src/lib/hooks/useGlobalDisplayPrefs.ts` — HIGH
- Same file as 1d. Whole file deletable; also delete the now-empty
  `src/lib/hooks/` directory.

### 4b. `src/components/display/DisplayTogglePill.tsx` — HIGH
- Same file as 2a. Whole file deletable; also delete the now-empty
  `src/components/display/` directory.

### 4c. `src/components/providers/ConsentGate.tsx` — HIGH
- Same file as 2b. Whole file deletable (leave the directory — other
  providers still live there).

---

## Cross-check notes

- `exportAsImage` is loaded via `await import("@/lib/utils/export-report")`
  in `src/app/page.tsx:488` — so the file itself stays, only remove the
  `exportAsPdf` export + `getJsPDF` helper.
- `hasAnalyticsConsent` / `onConsentChange` from `@/lib/consent` are actively
  used by `PostHogProvider`, `ClarityProvider`, and `CookieBanner`. Only the
  `ConsentGate` wrapper is dead.
- `isRateLimited` (sync) at `src/lib/rate-limit.ts:84` is only used by its
  own vitest suite. Kept out of the top-10 because deleting it would break
  the test file too and the comment says "kept for backward compatibility."

## Estimated LOC recoverable (top 3)

- `DisplayTogglePill.tsx` — 267 lines
- `useGlobalDisplayPrefs.ts` — 51 lines
- `ConsentGate.tsx` — 37 lines
- **Total**: ~355 lines + 2 empty directories, one-commit swarm safe.
