# R8 WCAG 2.1 AA static audit — 20-07-26

Scope: `src/app/{page,faq,support,privacy,terms,changelog,s,dashboard,explore}/**` plus persistent chrome. Static analysis only.

## CRITICAL — WCAG 2.1 AA blockers

1. **InstallPrompt is a modal without dialog semantics/focus management** — `src/components/ui/InstallPrompt.tsx:112-206`. Fixed backdrop + bottom sheet with no `role="dialog"`, no `aria-modal`, no focus trap, no Escape, no focus restore. Fails 2.1.2, 2.4.3, 4.1.2. Site-wide mobile impact after 60s.
2. **Dashboard 8-tab strip lacks tab semantics + color-only** — `src/app/dashboard/DashboardContent.tsx:163-197`. Plain `<button>`s, no `role="tablist"`, no `aria-selected`. **CONFLICT-RISK**.
3. **Explore chips signal state only via color** — `src/components/explore/ExploreFilters.tsx:285-298, 304-317`. No `aria-pressed`. Inconsistent with sibling filters in the same file that DO use `aria-pressed`. **CONFLICT-RISK**.
4. **Dashboard sort `<select>` has no accessible name** — `src/app/dashboard/DashboardContent.tsx:205-214`. No label/aria-label. **CONFLICT-RISK**.
5. **PageNavbar mobile bottom-nav lacks `aria-current="page"`** — `src/components/layout/PageNavbar.tsx:191-213`. Active state color-only. `aria-current` absent from entire codebase (0 hits). **CONFLICT-RISK**.

## HIGH

6. **NotificationBell < 44×44** — `src/components/ui/NotificationBell.tsx:109`. `p-2` around 16×16 = ~32×32. Every other icon-only button uses 44×44.
7. **Navbar overflow menu items lack `role="menuitem"` + Escape** — `Navbar.tsx:630-952`. **CONFLICT-RISK**.
8. **LanguageSelector not exposed as menu/listbox** — `LanguageSelector.tsx:28-88`. No aria-expanded/haspopup/current/Escape.
9. **Comment mod controls only visible on hover** — `CommentSection.tsx:285-309`. `focus-within` keeps them reachable but affordance invisible until focus.
10. **Champions banner dismiss ~24×24** — `PasteInput.tsx:312-321`. **CONFLICT-RISK**.
11. **Dashboard header pills ~28×28 on mobile** — `DashboardContent.tsx:144-158`. **CONFLICT-RISK**.
12. **`ShareModal` Copy divs use `role="button"` without focus-visible ring** — `ShareModal.tsx:416-423, 649-656`. Fails 2.4.7.
13. **Version-diff CloseIcon missing `aria-hidden`** — `page.tsx:1114-1121`. **CONFLICT-RISK**.

## MEDIUM

14. Dark-mode toggle SVG missing `aria-hidden` — `PageNavbar.tsx:171-175` and `Navbar.tsx:332-334, 344-346, 619-627`.
15. Regulation chip label "H" needs `aria-label="Reg H"` — `ExploreFilters.tsx:296`.
16. More-filters toggle missing `aria-controls` — `ExploreFilters.tsx:327-349`.
17. Several search/sort chevron SVGs missing `aria-hidden` — `ExploreFilters.tsx:233, 250, 337-342, 430`.
18. `WarningPopover` trigger missing aria-expanded/controls/Escape — `Navbar.tsx:154-160`.
19. Show-slide banner icon missing `aria-hidden` — `page.tsx:1197`.

## Already-fixed spot-checks

OTSSheetModal, ShareModal, WhatsNewModal, page.tsx export theme picker, PageNavbar "More", SlideNavControls, Navbar autosave status, layout.tsx skip-to-content, Button focus-visible, PokemonCard, ReportCard — all still in place.

## Conflict-risk overlaps

DashboardContent.tsx, ExploreFilters.tsx, PageNavbar.tsx, Navbar.tsx, PasteInput.tsx, page.tsx, ShareModal.tsx are on main-changed-files.md.

**Safe to edit (NOT on hot-file list)**: NotificationBell.tsx, InstallPrompt.tsx, LanguageSelector.tsx.

## Top 5 quick fixes (single commit each)

1. `InstallPrompt.tsx:122` — full dialog wrap + focus-trap copy from `ShareModal.tsx:155-201`. SAFE.
2. `DashboardContent.tsx:163-195` — role="tablist" + role="tab" + aria-selected. CONFLICT-RISK.
3. `ExploreFilters.tsx:285-317` — aria-pressed + aria-label on chips. CONFLICT-RISK.
4. `DashboardContent.tsx:205-208` — aria-label on sort select. CONFLICT-RISK.
5. `NotificationBell.tsx:109` — 44×44 target. SAFE.

## Actionable this run

Prefer SAFE fixes (1, 5) + LanguageSelector improvements. Defer conflict-risk to a follow-up run after main-branch changes settle.
