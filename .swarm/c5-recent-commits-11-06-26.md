# C5 Recent Commit Review — 11-06-26

Scope: last 20 commits on `main` (8eb39cc … 8021723), span 15-05-26 → 11-06-26.

## Hotspots (high-churn files)

- **src/app/api/webhooks/linear/route.ts** — touched in 7 of 20 commits (bcbda85, ae4b3b4, 6981f23, 709ca2d, 484fa50, 1d6c3de, 1a30839). Every single nightly swarm re-proposed the same `linear-signature` header / `LINEAR_WEBHOOK_SIGNING_SECRET` env var fix because the previous attempts never landed on `main`. The commit 1a30839 ("Merge swarm-nightly PRs #48/#49 + repair corrupted main") finally consolidated them. Risk: the underlying cause (swarm branches not actually merging) is not addressed by code; if the swarm pipeline regresses, we will see the same churn again.
- **src/app/changelog/ChangelogContent.tsx** — 11 of 20 commits. Mostly appending version entries plus the v5.13/v5.14 conflict resolution in cddad63. Expected behaviour for a changelog file, but it is also the file that has caused merge conflicts most often — see "Backlog candidates" below.
- **src/components/layout/Navbar.tsx** — 8 of 20 commits. Heavily reworked across the dock-deletion arc (3ace051 → 850e91c → 8eb39cc). The overflow menu now owns: Share, Save-to-my-reports, Mega toggle, presentation pencil, reorder, hide/show, keyboard shortcuts. Fragile area — one more accumulation cycle and this component will need a split.
- **src/components/ui/ShareModal.tsx** — 7 of 20 commits. Rental code card (ae4b3b4), native-share promotion (767ef07), aria-pressed/44px (90c57c2/7dd9900), aria-labelledby (bcbda85) — repeatedly poked but never refactored.
- **src/app/page.tsx** — 7 of 20 commits, and it is in the AVOID list. Flag only; no code change proposed.
- **src/lib/email.ts** — 6 of 20 commits. The escapeHtml story crossed three commits (6f1e552 added it for digests, 6981f23 reapplied it for welcome/comment, 709ca2d lifted it canonical, 1a30839 deduped). Now consolidated through `@/lib/utils/sanitize`. Fine, but the duplication scar is visible at src/lib/email.ts:8-12 with a re-export comment.

## Quick fixes (suggest changes)

- **src/lib/db/migrations/add-species-column.sql** — orphaned migration. The `species[]` column was added in 90c57c2/7dd9900 and dropped in 6981f23 (`drop-species-column.sql`). Both files still sit side-by-side under src/lib/db/migrations/. Suggested fix: delete `add-species-column.sql` (or move it into an `archived/` subfolder) so future devs do not re-apply a dropped column. The drop file references the history in its header so no comprehension is lost.
- **src/lib/email.ts:8-12** — vestigial re-export of `escapeHtml` with a "transition compat" comment. Once `import { escapeHtml } from "@/lib/email"` callsites are migrated to `@/lib/utils/sanitize`, drop the re-export. Quick grep to do: any non-`src/lib/email.ts` caller of `escapeHtml` from `@/lib/email` should be repointed.

## Backlog candidates (propose tickets)

- **[Infra] Stop the swarm-PR phantom-loop on webhook fix** — Eight consecutive nightly swarm runs (#32 through #47) re-proposed the same Linear webhook fix because the prior nightly PRs never merged to `main`. 1a30839 finally consolidated them but did not address the root cause. Ticket: investigate why nightly swarm PRs (#48, #49) sat unmerged, add a CI check that fails the swarm if last night's PR is still open, or rotate to "land within 24h or auto-close."
- **[Refactor] Navbar.tsx is becoming an everything-component** — 8 of 20 commits touched it; it now owns Share, Save, Mega toggle, pencil-to-edit, overflow sheet, reorder, hide/show, keyboard shortcuts. Propose: extract `NavbarOverflowMenu` and `NavbarPresentationControls` into separate components under `src/components/layout/navbar/`. ~No behavioural change; reduces churn surface.
- **[Investigate] Did 850e91c regress any share/reaction flow?** — 850e91c deleted ShareDock + FloatingReactionDock + useTouchIdleHide. 3ace051 (immediate predecessor) had just rewritten useTouchIdleHide and added DoubleTapLikeOverlay. This is the highest "looks like a revert of a recent fix" signal in the window: two weeks of dock UX investment was discarded based on user feedback. Risk: any in-flight PostHog dashboards tracking share-dock-click / reaction-dock-click events now go to zero — confirm event renames are wired (and the cleanup ticket landed) before the next product review.
- **[Tech debt] Newsletter half-migration** — 52437b8 deleted the public NewsletterSignup component and `/api/newsletter` route but explicitly left the Neon `newsletter_subscribers` table un-dropped ("run the DROP manually in the Neon SQL editor when ready"). Ticket: write the `drop-newsletter-subscribers.sql` migration, run it, and remove the row in CLAUDE.md / changelog notes that warns of the orphan table. Currently this is a footgun for the next dev who searches the schema and sees an unused table.
- **[Tech debt] Pikalytics removal partial** — ae4b3b4 (VGC-211) removed UI references to Pikalytics after 484fa50 removed the parser. Confirm no stragglers in `src/lib/utils/multi-import.ts` types, sitemap entries, or SEO copy. Quick audit ticket, likely 30 min.
- **[Followup] Standardise to one Linear env var name** — Multiple commits (ae4b3b4, 6981f23, bcbda85, 709ca2d) add `LINEAR_WEBHOOK_SIGNING_SECRET` with `LINEAR_WEBHOOK_SECRET` as a fallback "until renamed in Vercel." The legacy fallback is still live in src/app/api/webhooks/linear/route.ts. Once Vercel env is confirmed renamed, drop the fallback in a small PR.
- **[Followup] Auto-presentation mode UX** — b1af62f auto-enters presentation mode on shared views (skipping `?key=`). 3ace051 then added a pencil icon in the Navbar so owners can escape it. The combination is functional but unfollowed by analytics. Ticket: add PostHog events for `auto_presentation_entered`, `auto_presentation_exited_via_pencil` so we can see how often owners are surprised by it.
- **[Quality] eslint-disable-line audits** — Two `eslint-disable @next/next/no-img-element` lines were added in 90c57c2/7dd9900/709ca2d (sprite images). These are reasonable (Pokemon sprites are external CDN, not optimisable through next/image), but should be revisited if/when we self-host sprites. Tag the disables with a `// VGC-XXX revisit when sprites are bundled` comment.

## Style consistency note

- All 50 API routes use `NextResponse.json()` consistently. Only `src/app/api/share/[id]/route.ts` and `src/app/api/sync/[id]/route.ts` mix in `new Response(...)` — both legitimate (304 Not Modified with no body, streaming response). No action needed.
- All admin/cron routes (`migrate`, `cleanup`, `setup`, `bot`, `keep-alive`, webhooks) now route bearer auth through `verifyBearer()` from `src/lib/auth/verify-bearer.ts`. Migration complete — no stragglers.

## Summary (120 words)

The last 20 commits are dominated by a swarm-pipeline failure mode: the Linear webhook fix was re-proposed in eight consecutive nightly runs (touching one route 7x) before 1a30839 finally consolidated them and repaired corrupted files left behind by botched prior merges. Highest-value backlog item is fixing the swarm pipeline itself, not the code. Secondary concern: Navbar.tsx (8/20 commits) is absorbing every dock/overlay change and should be split. Two genuine partial migrations exist — the `newsletter_subscribers` Neon table (un-dropped by 52437b8) and an orphaned `add-species-column.sql` migration (column was added then dropped). Email escapeHtml work is now consolidated; admin-route bearer auth is fully migrated; API response style is consistent. No reverts of real fixes detected; 850e91c's dock deletion is a deliberate product call, not a regression.
