# R8 Accessibility Quick Wins — 2026-07-27

15 findings — all one-line fixes except the InlinePokemonEditor focus-trap and the nested-clickable in MatchupPlanSlide.

## Disclosure buttons missing aria-expanded
- `src/components/ui/LanguageSelector.tsx:28` — add `aria-expanded={isOpen}` and `aria-haspopup="menu"`
- `src/components/report/MatchupPlanSlide.tsx:500` — add `aria-expanded={!isCollapsed}`
- `src/components/report/TeamOverview.tsx:239` — add `aria-expanded={open}` + `aria-haspopup="dialog"` (Legal badge)
- `src/components/report/TeamOverview.tsx:265` — add `aria-expanded={open}` (issues-count badge)
- `src/components/layout/Navbar.tsx:154` — add `aria-expanded={open}` + `aria-haspopup="dialog"` (WarningPopover)

## Toggle state missing aria-pressed
- `src/components/layout/PageNavbar.tsx:166` — add `aria-pressed={darkMode}` on dark-mode toggle

## Decorative SVGs need aria-hidden
- `src/app/page.tsx:836` — decorative X icon inside error bubble
- `src/components/layout/Navbar.tsx:618` — cog SVG inside Settings button (parent already aria-labelled)
- `src/components/report/PokemonCard.tsx:307` — pencil SVG inside labelled Replace button

## Contrast failures (1.4.3)
- `src/app/champions/ChampionsContent.tsx:144,167` — `text-white/70` and `text-white` on light type badges (Electric #F8D030, Ice #98D8D8) fail AA. Switch to `text-black` for the light-type set.

## Deeper fixes (deferred — larger blast radius)
- `src/components/report/MatchupPlanSlide.tsx:553` — nested `<span onClick>` inside `<button>` is invalid interactive nesting. Needs to become a sibling button.
- `src/components/report/InlinePokemonEditor.tsx:96` — dialog lacks focus-restore-on-close, focus trap, and `aria-modal`.
- `src/components/report/SlideNavControls.tsx:511` — slide-dot buttons 20x20px — below WCAG 2.5.5 44px minimum on mobile.
- `src/components/report/PokemonDetailSlide.tsx:928` — Set/Stats/Notes/Calcs tab bar missing `role="tablist"` semantics.

## Actions taken this run
Applying the aria-expanded/aria-pressed/aria-hidden and contrast fixes — all one-line changes, low risk.
