# R6 SEO Audit — 2026-06-04

Read-only Explore agent. Summary of findings captured here; many were implemented inline this run.

## Top 10 quick wins

| # | File | Issue | Status this run |
|---|---|---|---|
| 1 | `src/app/sitemap.ts` | Duplicate `/compare` entry with conflicting priorities 0.6 and 0.5 | ✅ Fixed (commit `ba0c2f1`) |
| 2 | `src/app/compare/page.tsx` | Missing `alternates.canonical` | ✅ Fixed (commit `ba0c2f1`) |
| 3 | `src/app/layout.tsx` | Root canonical | Already present at line 84 — no action needed |
| 4 | `src/app/{feedback,privacy,terms,changelog}/page.tsx` | Missing `keywords` metadata | Deferred — `keywords` is largely a Bing/legacy signal; low impact. Other commits already raise these pages' authority via Breadcrumb JSON-LD |
| 5 | `src/app/compare/page.tsx` | Conflicting robots `{ index: false, follow: true }` | ✅ Fixed (commit `ba0c2f1`) — switched to `{ index: false, follow: false }` |
| 6 | `src/app/s/[id]/page.tsx` | Minimal share-page schema (no breadcrumbs/dates) | Deferred — `s/[id]` was touched recently on main; high conflict risk |
| 7 | `src/app/embed/[id]/page.tsx` | No schema on embed pages | Skipped — embeds are explicitly `noindex,nofollow` by design |
| 8 | Pokemon sprites | Generic `alt={species}` | Deferred — would need to touch multiple report components in conflict-risk file list |
| 9 | `src/app/{feedback,privacy,terms,compare,notifications}/page.tsx` | Missing BreadcrumbList JSON-LD | ✅ Fixed on /compare /feedback /privacy /terms (commit `ba0c2f1`). `/notifications` deferred — recently touched on main |
| 10 | `public/robots.txt` | No `Disallow: /dashboard/` rule | ✅ Fixed (commit `ba0c2f1`) |

## Wins shipped tonight

5 of 10 → main code commit `ba0c2f1`. The remaining 5 are either lower-impact (keywords), high conflict risk (recently touched on main), or already in place.

## Out of scope tonight

- Live-site WebFetch — explicitly skipped per the prompt (too slow / 403 from the production WAF for unauthenticated WebFetch).
- Per-page meta description tightening — deferred to a focused R6 follow-up that can verify each new description against existing copy without risking regression.
