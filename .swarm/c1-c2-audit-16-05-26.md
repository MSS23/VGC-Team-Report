# Dead Code + TypeScript Audit — 16-05-26

## Dead exports found (14)

### `src/lib/analysis/stat-calculator.ts` (4 exports, zero external imports)
- `evSpreadToSp` — converts EV StatSpread to SP StatSpread; never imported outside the file
- `spToEvSpread` — inverse of above; never imported
- `formatSpSpread` — formats SP spread to string; never imported
- `isChampionsOptimized` — checks if EV spread is optimized; never imported

### `src/lib/utils/mega-detect.ts` (2 exports, zero external imports)
- `getBaseFormName(megaDataKey)` — strips Mega suffix to get base species; never imported
- `getMegaDataKey(species, item)` — builds lookup key from species+item; never imported

### `src/lib/linear.ts`
- `isLinearConfigured()` — convenience boolean check; never imported anywhere in app code

### `src/lib/utils/multi-import.ts`
- `importTeam(input, fetchPokePaste)` — async team importer abstraction; never imported (callers use `detectImportSource` + `fetchPokePaste` directly)

### `src/lib/utils/game-plan-helpers.tsx` (2 exports, zero external imports)
- `ResultBadge` — React component for W/L/T badge; only `GAME_COLORS`, `getReplayInfo`, `ReplayIcon` are imported from this module
- `ResultToggle` — React component for toggling W/L/T; same situation

### `src/lib/data/tags.ts`
- `EventType` (type alias) — `export type EventType = (typeof EVENT_TYPES)[number]`; no file imports this type, callers use raw `string`

### `src/hooks/useExploreUrlSync.ts`
- `ExploreUrlSyncResult` (interface) — the hook's own return type annotation; never imported by consumers (they rely on inference)

### `src/lib/utils/pokepaste.ts` (2 interface-level dead exports)
- `PokePasteResult` — return type of `fetchPokePaste`; no external code imports this interface
- `CreatePokePasteInput` — parameter type of `createPokePaste`; used only inside `pokepaste.ts` itself

---

## TypeScript `any` uses (2)

| File | Line | Usage |
|------|------|-------|
| `src/lib/utils/normalize-report.ts` | 8 | `type AnyRecord = Record<string, any>` — used for legacy migration shim; has eslint-disable comment |
| `src/app/api/migrate/route.ts` | 50 | `const data = row.data as Record<string, any>` — DB row cast; has eslint-disable comment |

Both are intentional/suppressed. The `normalize-report.ts` case is the higher-priority fix: `AnyRecord` could be narrowed to `Record<string, unknown>` without breaking usage since `normalizeReportData` already handles coercion internally.

---

## Missing return types (12 exported functions in `src/lib/`)

| File | Function | Notes |
|------|----------|-------|
| `src/lib/db.ts` | `getDb()` | Returns Neon SQL client — inferred but not annotated |
| `src/lib/db.ts` | `ensureTable()` | Should be `Promise<void>` |
| `src/lib/email.ts` | `sendEmail(opts)` | Returns `Promise<null \| unknown>` — should be explicit |
| `src/lib/email.ts` | `sendCommentNotificationEmail(opts)` | Should be `Promise<void>` |
| `src/lib/discord-webhook.ts` | `postToBuildsChannel(embed)` | Should be `Promise<void>` |
| `src/lib/discord-bot.ts` | `postFeedbackEmbed(opts)` | Should be `Promise<unknown \| null>` |
| `src/lib/notifications.ts` | `createNotification(...)` | Should be `Promise<void>` |
| `src/lib/notifications.ts` | `notifyFollowers(...)` | Should be `Promise<void>` |
| `src/lib/utils/haptics.ts` | `hapticLight()` | Should be `void` (trivial but inconsistent) |
| `src/lib/utils/haptics.ts` | `hapticMedium()` | Should be `void` |
| `src/lib/utils/haptics.ts` | `hapticSuccess()` | Should be `void` |
| `src/lib/i18n/index.ts` | `useTranslation()` | Returns context value — inferred but not annotated |
| `src/lib/i18n/index.ts` | `I18nProvider(...)` | Should be `JSX.Element` |
| `src/lib/hooks/useGlobalDisplayPrefs.ts` | `useGlobalDisplayPrefs()` | Return type inferred but not annotated |

---

## Unused imports (2 confirmed)

| File | Unused Import | Note |
|------|--------------|-------|
| `src/components/layout/Navbar.tsx:13` | `hapticMedium` from `@/lib/utils/haptics` | Only `hapticLight` is used in this file |
| `src/hooks/useShareUrl.ts:6` | `encodeShareState` from `@/lib/sharing/url-codec` | Imported but never called; `decodeShareState` is used |

---

## Recommended quick wins

1. **Remove `encodeShareState` from `useShareUrl.ts`** — clean unused import, zero risk.
2. **Remove `hapticMedium` from `Navbar.tsx` import** — clean unused import.
3. **Delete `importTeam` from `multi-import.ts`** — entire function dead; callers handle the flow themselves.
4. **Delete `ResultBadge` and `ResultToggle` from `game-plan-helpers.tsx`** — no consumers; consider moving to `MatchupPlanSlide.tsx` if they'll be needed in future.
5. **Narrow `AnyRecord` in `normalize-report.ts`** to `Record<string, unknown>` — removes the only `any` in `src/lib/`.
6. **Add `Promise<void>` return types** to `createNotification`, `notifyFollowers`, `postToBuildsChannel`, and the three haptic functions — lowest-effort return type wins with highest coverage.
7. **Trim 4 dead exports from `stat-calculator.ts`** (`evSpreadToSp`, `spToEvSpread`, `formatSpSpread`, `isChampionsOptimized`) — Champions SP path these serve is currently unused in render code.
