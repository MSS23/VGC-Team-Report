# R8: Accessibility Audit — Run 2026-06-10

## Overall: PASS — No critical WCAG violations

**Strengths:**
- All icon-only buttons have proper aria-labels (EditFab, NotificationBell, DiffNavigator, modals)
- Global focus-visible styling with 2px accent ring is excellent
- Modal implementations (WhatsNewModal, ShareModal, OTSSheetModal) have proper focus trap, escape-to-close, focus restore
- Interactive state indicators use multiple affordances (color + icon + text + borders), not color-only
- Form inputs use aria-label or placeholder appropriately

## Issues Found

### Medium-priority
1. **ShareModal `<div role="button">`** at lines 395-409 and 651-665 should be native `<button>` elements
2. **5 decorative SVGs lack `aria-hidden="true"`** in ThemePicker, WhatsNewModal, and badges
3. **2 modals (ShareModal, OTSSheetModal) missing `tabIndex={-1}` on dialog root** — semantic refinement

### Low-priority
4. **Runtime heading hierarchy** — requires browser DevTools verification
5. **44×44px touch targets** — already compliant per code review

## Top 5 Impact Wins
1. Add `aria-hidden="true"` to 5+ decorative SVGs (ThemePicker, WhatsNewModal badges)
2. Replace `<div role="button">` with native `<button>` in ShareModal (2 instances)
3. Add `tabIndex={-1}` to modal dialog roots (ShareModal, OTSSheetModal)
4. Audit runtime heading hierarchy in browser DevTools
5. Ensure all form inputs maintain 44×44px minimum touch targets (already compliant)

## Conflict-Risk Check
None of the top 5 are in `.swarm/main-changed-files.md`.
