# L0 Linear triage — 14 Sep 2026 (run INLINE, not delegated — saves 1 dispatch)

Linear MCP is unauthenticated in this session, so triage used the Linear GraphQL
API directly. 108 open issues pulled and paginated in full.

## Board shape
| State | Count | Note |
|---|---|---|
| In Review | 32 | stale — belongs to 5 unmerged draft PRs |
| Backlog | 62 | 46 carry `auto-research` (swarm-generated), 24 carry `no-claude` |
| Todo | 13 | |
| In Progress | 0 | |
| Bugs outside In Review | **0** | every open Bug is already implemented and waiting on a merge |

## The finding that matters most
There is no implementation backlog to drain. The small, safe, well-specified work
has ALREADY been done by previous runs and is sitting in 5 unmerged draft PRs
(#72 03-08, #74 17-08, #75 24-08, #76 31-08, #77 07-09). `main`'s last commit is
24-08-26 — three weeks stale.

GOAL A ("drain the board") is therefore blocked on the human merge step, not on
subagent throughput. Implementing 10 more tickets onto a 6th unmerged branch
would move zero tickets to Done. Already filed as VGC-265 (P1, 10-08-26),
which is now understated: it says "~30 branches / 10 tickets", actual is
32 tickets In Review.

## Todo (13) — why most are NOT implementable unattended
| Ticket | P | Verdict |
|---|---|---|
| VGC-41 Full Champions format support | P1 | TOO LARGE — dex + mechanics + import, multi-day |
| VGC-207 Anonymous quick-share paste-to-URL | P2 | TOO RISKY — bypasses Clerk auth + new DB writes |
| VGC-200 Notification prefs to DB | P2 | NEEDS MIGRATION — CLAUDE.md classes migrations as large/risky |
| VGC-50 EV spread builder | P2 | TOO LARGE — new interactive builder |
| VGC-43 6v6 threat/coverage matrix | P2 | TOO LARGE |
| VGC-40 Limitless tournament import | P2 | EXTERNAL DEP — Limitless API unreachable (egress policy) |
| VGC-38 Inline damage calculator | P2 | TOO LARGE |
| VGC-162 Root page.tsx Server Component refactor | P3 | RISKY — touches src/hooks/useHomePage.ts, a conflict-risk file |
| VGC-158 Decompose PokemonDetailSlide (962 ln) | P3 | LARGE mechanical refactor, subtle UI-break risk unattended |
| VGC-129 Push notifications | P3 | EXTERNAL DEP — needs VAPID keys (human/env) |
| VGC-48 Real-time collaboration | P3 | TOO LARGE |
| VGC-187 PWA screenshots | P3 | CANDIDATE — additive binary assets |
| VGC-136 Home button in subcategories | P3 | `no-claude`, non-bug -> SKIPPED per guardrail |

## Backlog — excluded categories
- 24 `no-claude` non-bug tickets: skipped per guardrail.
- 10 growth/outreach tickets (VGC-66, 67, 71, 85, 88, 90, 107, 113, 147, 148):
  every one resolves to "DM / post / submit / publish / launch". The NO OUTBOUND
  SENDING guardrail makes these drafts-only — NOT implementable. Not counted
  against GOAL A.
- VGC-215 (run SQL against production) and VGC-253 (DB column): production DB
  work, human-only.
- VGC-236 (drop legacy webhook env var): BLOCKED — see .swarm/webhook-investigation.md §6.
  Dropping the fallback without Vercel read access could take the webhook from
  intermittently failing to failing 100%.

## Selected for implementation (additive, low blast radius)
Chosen for being ADDITIVE — new files/routes rather than edits to hot paths —
which is what makes them safe to land unattended.
1. VGC-217 P2 Per-species EV-spread landing pages (SSG, largest keyword gap)
2. VGC-227 P3 /blog/[slug] route scaffold (route only; posts stay drafts)
3. VGC-226 P2 Rental-code field + copy-to-clipboard
4. VGC-187 P3 PWA screenshots
Plus quick-win fixes surfaced by Wave 1 (a11y, type-safety, security, perf).

## File-overlap control
No two selected items share a file. VGC-217 and VGC-227 both add new route
directories under src/app/ but no shared file. Conflict-risk list
(.swarm/main-changed-files.md) is only 3 files, none touched by the above.
