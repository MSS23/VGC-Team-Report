# C5 review — last 20 commits on `main` (20-07-26)

## High-risk commits — Follow up on these first

- **83d195a** — `§7.6: add lastModified to champion sitemap pages`
  - Commit message admits the code change is a *build-trigger hack* to defeat Vercel's Ignored Build Step. Bundling a semantic edit with a CI workaround pollutes bisect. Follow-up: configure the Vercel Ignored Build Step to always run (or use a path filter).

- **359cdef** — `Secure report editing and exit-safe drafts` (30 files, +411/-499)
  - `src/app/api/share/[id]/collaborators/route.ts:156-191` (`PATCH`) is unreachable from UI; docstring says "retained for incident recovery" but no admin surface exists. Delete or expose.
  - `editKeyFromUrl` still plumbed through `useShareUrl.ts:74,226,430`, `useShareFlow.ts:30,169`, `useHomePage.ts:801`, `page.tsx:150,798` despite code comment "it has no bearing on access." Half-wired refactor.
  - `useShareUrl.ts` effect ~line 135 resets refs BEFORE aborting on `!isAuthLoaded` — UI flash risk when Clerk is slow to load.

- **fe70914** — `VGC-64: refresh public sitemap hourly` (`src/app/sitemap.ts`)
  - `lastModified: now` was removed from every static entry (home, /explore, /champions, etc.) — SEO regression. Fix: keep `lastModified: now` on static pages (revalidate = 3600 makes the timestamp refresh naturally).
  - Creator query hard-caps at 5000; file a ticket to paginate at ~50k.

- **0269462** — `Improve mobile UX and sharing journey` (42 files)
  - **User-facing default change buried**: `src/hooks/useShareFlow.ts` — `isPublic` default flipped `true → false`, `isUnlisted` default flipped `false → true`. New reports now default to unlisted. Not in the commit message or changelog. Add a Linear ticket to communicate it.
  - `next.config.ts` — `'unsafe-eval'` added to `script-src` in dev only (correct scoping).
  - `src/proxy.ts` matcher comment mentions Next 16's URL Pattern parser rejecting the second matcher; sprite exclusion is now body-level only.

## Refactor debt — worth a Linear ticket

- **359cdef fallout:** delete or expose the unreachable `PATCH /api/share/[id]/collaborators` handler; rip `editKeyFromUrl` out of `useShareUrl`, `useShareFlow`, `useHomePage`, `page.tsx`, and test doubles.
- **359cdef `SHARE_TOKENS_KEY`:** kept only to `localStorage.removeItem` on mount (`src/hooks/useShareUrl.ts:109`) — schedule removal after 90 days.
- **d3260c6** — `useAutoDraft` `keepalive: true` on exit save has ~64 KB body cap in most browsers — big drafts may silently fail on tab close. Confirm behaviour or use `navigator.sendBeacon`.
- **0635b74** — `src/lib/fetch-with-timeout.ts` docstring warns "Any signal you pass is replaced by the internal timeout signal." Take an optional external signal and `AbortSignal.any([...])` them.
- **7349ac7** — commit bundles unrelated concerns (multilingual moves + `/support` + 485-line Stripe doc + silent flag emoji removal). Split future commits.

## Minor concerns — nits, style, small clean-ups

- **14273a1** — swaps `next/link` `<Link>` for raw `<a>` in `page.tsx` and `Navbar.tsx` to force reloads. Loses prefetch. Consider `router.push('/')` with explicit state reset.
- **fbfc877** — `CommonModesSlide.tsx` grew from ~60 to ~515 lines in one commit; break into subcomponents.
- **cfcbd2f** — `resolveSpeedTierForm` uses a local `toSpeciesKey` that duplicates the app-wide slug helper (`sprite-slug.ts`); consolidate.
- Pre-existing `eslint-disable-next-line react-hooks/exhaustive-deps` in `src/hooks/useHomePage.ts:90,219` still in place.

## Clean — nothing to flag

c51a9e1, cba0832, 470dfa2, 039fd41, 2a24ae9, ede31b4, c3d50c1, d706f71, 0825946.

## Top 3 concerns summary

1. **83d195a** — sitemap commit is a build-trigger hack; fix Vercel Ignored Build Step config.
2. **359cdef** — 80% complete refactor: dead PATCH endpoint + vestigial `editKeyFromUrl` plumbing.
3. **0269462** — silently flipped new-report default from Public to Unlisted; needs What's New surfacing.

## Actionable this run
- Fix sitemap `lastModified` regression on static entries (small, safe).
- Remove vestigial `editKeyFromUrl` plumbing (needs care — touches several hooks).
- Consolidate `toSpeciesKey` into `sprite-slug.ts` (small, safe).
