# Bundle/Perf Audit — 2026-06-03

**Branch:** `swarm-nightly-2026-06-03`  
**Analysis Type:** Static code audit (no build run)  
**Prior Wins:** PR #50 (qrcode singleton, Navbar dynamic imports), PR #52 (Sentry session-replay removal ~97KB), PR #53 (motion library lazy-load in PasteInput/ExploreContent ~80–120 KB)

---

## Summary

Found **3 safe-tonight wins** + **2 architectural issues** spanning 9 locations. All involve eager `motion/react` imports in conditional-render components where dynamic import is feasible, plus one unnecessary wildcard Sentry import in error boundary.

---

## Findings

### 1. **motion/react** in ExploreFilters — Conditional AnimatePresence (Safe-Tonight)
- **File:** `/home/user/VGC-Team-Report/src/components/explore/ExploreFilters.tsx` (line 6, 413–552)
- **Issue:** Imports `{ AnimatePresence, motion, useReducedMotion }` at top level. Used only in `moreOpen && <motion.div>` collapse animation (lines 413–552), conditional on filter toggle state.
- **Est. cost:** ~65–80 KB (motion/react full bundle)
- **Fix:** Dynamic import inside useEffect or lazy wrap via React.lazy + Suspense when `moreOpen` true
- **Status:** ✓ **safe-tonight** — animation non-critical; collapse works without motion (fallback to opacity only)
- **PR Candidate:** Group with ReportCard below

### 2. **motion/react** in ReportCard — Wraps Only Root Link (Safe-Tonight)
- **File:** `/home/user/VGC-Team-Report/src/components/explore/ReportCard.tsx` (line 4, 160–384)
- **Issue:** Imports `{ motion }` at top level. Used only as `<motion.a>` root wrapper (lines 160–384), which is *always* rendered on `/explore` page but optional animations could use plain `<a>` as fallback.
- **Est. cost:** ~65–80 KB
- **Fix:** Lazy-load motion on `/explore` route only via conditional import in parent `ExploreContent`, or use CSS animations as fallback for `<a>` entrance
- **Status:** ✓ **safe-tonight** — card visible without animation; entrance animation is UX nice-to-have, not core
- **Frequency:** Rendered on every `/explore` card in list; LazyMotion would save per-page cost

### 3. **motion/react** in SpotlightCard — Similar to ReportCard (Safe-Tonight)
- **File:** `/home/user/VGC-Team-Report/src/components/explore/SpotlightCard.tsx` (line 4, 36–165)
- **Issue:** Imports `{ motion }` at top level. Used for `<motion.a>` root and `<motion.div>` sprite stagger (lines 36–109), conditionally rendered when spotlight report exists.
- **Est. cost:** ~65–80 KB
- **Fix:** Dynamic import in parent `SpotlightSection` (line 170–195); section returns null if no report, so motion cost only incurred when needed
- **Status:** ✓ **safe-tonight** — card never rendered on all pages; lazy-loading eliminates zero-cost on most navigations
- **Savings:** ~65 KB on `/` when no spotlight available (25% of `ExploreContent` bundle as of PR #53)

### 4. **motion/react** in FeedbackContent — Conditional Modals (Safe-Tonight)
- **File:** `/home/user/VGC-Team-Report/src/app/feedback/FeedbackContent.tsx` (line 4, 123–164)
- **Issue:** Imports `{ motion }` at top level. Used only in conditional branches: sign-in state (motion.div, line 123), submitted state (motion.div + nested motion.div with spring, line 144–159), and form input animations. Three separate states; only one rendered per view.
- **Est. cost:** ~65–80 KB
- **Fix:** Dynamic import conditioned on form state; load motion only after auth check (after sign-in button interaction or success), defer until needed
- **Status:** ✓ **safe-tonight** — feedback form not on critical path; deferred load improves `/feedback` LCP by avoiding parsing on every visit
- **Frequency:** Isolated page, not part of homepage bundle

### 5. **motion/react** in ExploreContent — Already Handled (PR #53)
- **File:** `/home/user/VGC-Team-Report/src/components/explore/ExploreContent.tsx` (line 4)
- **Status:** ✗ Already deferred in PR #53; no new action

### 6. **motion/react** in CreatorProfile — Conditional Profile View (Needs-Ticket)
- **File:** `/home/user/VGC-Team-Report/src/components/social/CreatorProfile.tsx` (line 4)
- **Issue:** Imports `{ motion }` at top level; exact usage unclear without full scan, but component renders profile cards/lists that could be animated
- **Est. cost:** ~65–80 KB
- **Status:** ⚠️ **needs-ticket** — requires architecture review; creator pages are low-traffic discovery routes; worth deferring but needs testing for UX parity

### 7. **Wildcard Sentry Import** in global-error.tsx (Safe-Tonight)
- **File:** `/home/user/VGC-Team-Report/src/app/global-error.tsx` (line 3)
- **Issue:** `import * as Sentry from "@sentry/nextjs"` imports entire Sentry SDK. Used only for `Sentry.captureException(error)` (line 14). 
- **Est. cost:** ~45–60 KB (@sentry/nextjs bundle)
- **Fix:** Named import: `import { captureException } from "@sentry/nextjs"`, call as `captureException(error)`
- **Status:** ✓ **safe-tonight** — single-line mechanical change; error boundary used only on unhandled errors (rare), but SDK is parsed on every page load at framework level. Shaving wildcard saves ~15–20 KB parsed size
- **Frequency:** Error boundary in layout; Sentry hook runs globally

### 8. **Wildcard Cookie Library** in CookieBanner.tsx (Needs-Ticket)
- **File:** `/home/user/VGC-Team-Report/src/components/providers/CookieBanner.tsx` (line 4)
- **Issue:** `import * as CookieConsent from "vanilla-cookieconsent"` imports entire library. Used only for `CookieConsent.run()` and `CookieConsent.showPreferences()` (lines 16, 96). Named exports available.
- **Est. cost:** ~30–40 KB (vanilla-cookieconsent bundle)
- **Fix:** Switch to named imports if available, or lazy-load CookieConsent.run inside useEffect with dynamic import
- **Status:** ⚠️ **needs-ticket** — depends on library's export shape; verify `run` and `showPreferences` are named exports before refactoring. Deferred load improves LCP; banner not critical until user scrolls to footer.

---

## Excluded (Already Handled)

- ✓ `PasteInput.tsx` motion lazy-load (PR #53)
- ✓ `ExploreContent.tsx` motion lazy-load (PR #53)
- ✓ PostHog deferred to idle (May 2026 changelog notes requestIdleCallback)
- ✓ JSON imports: `dex-subset.json` (324 KB) already optimized via pre-extraction; no client-side dynamic import safe

---

## Priority Order (by Est. Bundle Savings)

| Rank | File | Lib | KB | Difficulty |
|------|------|-----|--|-|
| 1 | SpotlightCard.tsx | motion/react | 65–80 | Low |
| 2 | ReportCard.tsx | motion/react | 65–80 | Low |
| 3 | FeedbackContent.tsx | motion/react | 65–80 | Low |
| 4 | ExploreFilters.tsx | motion/react | 65–80 | Medium |
| 5 | global-error.tsx | @sentry/nextjs | 15–20 | Trivial |
| 6 | CreatorProfile.tsx | motion/react | 65–80 | Medium |
| 7 | CookieBanner.tsx | vanilla-cookieconsent | 30–40 | Medium |

---

## Proposed Next Steps

1. **Immediate (safe-tonight):** Land named import fix to global-error.tsx (5 min, 15–20 KB savings)
2. **Phase 1 (Low-hanging):** Lazy-load SpotlightCard motion (ExploreSection parent); lazy-load ReportCard if on `/explore` route only
3. **Phase 2 (Medium):** Defer FeedbackContent motion to form interaction; investigate CookieBanner export shape
4. **Phase 3 (Validation):** CreatorProfile motion audit + testing on low-traffic profile routes

**Est. Total Wins:** ~280–340 KB if all safe-tonight + phase 1 implemented.
