# L0 Linear Triage — 2026-08-10 (run inline by the orchestrator, no dispatch slot spent)

Board snapshot: **96 open issues** — Backlog 72, Todo 13, **In Review 10**, Duplicate 1.

## ⚠️ The most important board signal tonight

**10 tickets are sitting in In Review**, and PR #72 (`swarm: nightly improvements 03-08-26`)
has been open since 03 Aug. There are ~30 unmerged `swarm-nightly-*` branches on origin.
`src/app/changelog/data.ts:70` literally reads *"8th consecutive fix proposal — please merge!"*.

The board is not blocked on implementation capacity. It is blocked on **merges**.
Draining it further tonight has diminishing returns until the existing In Review work lands.
This is flagged at the top of the PR body and the Discord notification.

Already In Review — deliberately NOT re-implemented tonight (their code lives on unmerged
branches; redoing them would create conflicting duplicate implementations):
VGC-224, VGC-181, VGC-245, VGC-251, VGC-64, VGC-219, VGC-242, VGC-243, VGC-246, VGC-247.

## Selection rules applied
- Bugs first, regardless of the `no-claude` label.
- Non-bug tickets carrying `no-claude` skipped entirely (VGC-263, 252, 250, 249, 244, 241, 240,
  239, 238, 136, 60, 57, 58, 59, 56, 62, 63, 21, 16, 7, 6, 184, 18).
- Skipped anything requiring outbound network, a human dashboard action, or a production DB write —
  the container has no egress (VGC-255) and no DB credentials.
- Skipped anything whose right action is to send/post/publish (marketing tickets) per the
  no-outbound-sending guardrail.

## Selected for implementation tonight

| Ticket | P | Title | Files likely to change | Conflict risk |
|---|---|---|---|---|
| VGC-260 | P3 **Bug** | version-diff emits `pokemon:<index>`, parses as species name | `src/lib/utils/version-diff.ts` + new `__tests__` | no |
| VGC-254 | P1 | Privacy policy omits Microsoft Clarity session recording | `src/app/privacy/page.tsx` | no |
| VGC-262 | P2 | Standalone EV→SP converter page (SEO) | new `src/app/tools/ev-to-sp/**`, `sitemap.ts` | no |
| VGC-261 | P3 | Enable 4 already-clean TS strict flags | `tsconfig.json` | held for C2 |
| VGC-258 | P2 | /champions index grid missing the 14 Reg M-B megas | `src/app/champions/ChampionsContent.tsx` | held for R6 |
| VGC-259 | P2 | a11y: no `<h1>` on report slides after the first | `src/components/report/TeamReport.tsx` | held for R8 |
| VGC-256 | P2 | Lazy-load zod out of the client bundle | `src/lib/sharing/url-codec.ts`, `useShareUrl.ts` | held for C3 |
| VGC-257 | P2 | dex-subset.json eagerly bundled into homepage | `src/lib/data/**`, `mega-detect` | **yes — overlaps VGC-256** |

Four tickets were held back from the first batch because a read-only Wave 1 auditor was actively
reading the exact files they would rewrite (C2↔tsconfig, R6↔champions, R8↔report components,
C3↔bundle). They are dispatched in Batch B once those auditors return, which also means each
implementer inherits a freshly verified finding instead of a three-week-old ticket description.

## Deferred, with reasons (NOT silently dropped)

- **VGC-226** (rental-code field) — requires a `shares` table migration (`rental_code TEXT NULL`).
  No DB credentials in the container, so the migration cannot be applied or verified. Shipping the
  UI against a column that does not exist would break the editor. Needs a human-run migration first.
- **VGC-232** (sprite-fallback CDN proxy) — the whole point is falling back when an upstream sprite
  CDN 404s. With egress blocked, neither the primary nor the fallback path can be exercised, so
  "verified" would be a lie. Deferred until VGC-255 is fixed.
- **VGC-236** (drop legacy `LINEAR_WEBHOOK_SECRET`) — gated on confirming the Vercel env var name.
  Vercel unreachable. Dropping the fallback blind could break a currently-working webhook.
- **VGC-248 / VGC-221** (breaking dependency upgrades) — major-version bumps of Clerk and others are
  exactly the "large/risky" class CLAUDE.md says needs its own branch and human review.
- **VGC-215 / VGC-213 / VGC-222 / VGC-220 / VGC-255** — all require a human at a dashboard
  (Vercel env, Linear settings, production SQL, container config).
- **VGC-187** (PWA screenshots) — needs real screenshots captured from a running app.
- **VGC-162 / VGC-158 / VGC-207 / VGC-217 / VGC-225** — genuinely multi-session refactors/features,
  above the <4h-per-ticket bar for an unattended run.
