---
phase: 02-advanced-filter-drawer
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/explore/ExploreFilters.tsx
  - src/components/explore/AdvancedFilterDrawer.tsx
autonomous: false
requirements:
  - UX-01
  - UX-04
  - UX-05

must_haves:
  truths:
    - "Drawer works correctly on both mobile and desktop viewports"
    - "Filters apply instantly when changed in the drawer"
    - "Primary bar is decluttered and only shows high-frequency filters"
  artifacts:
    - path: "src/components/explore/AdvancedFilterDrawer.tsx"
      provides: "Verified working drawer component"
    - path: "src/components/explore/ExploreFilters.tsx"
      provides: "Verified refactored filter bar"
  key_links:
    - from: "AdvancedFilterDrawer"
      to: "ExploreContent fetchReports"
      via: "filter callback props trigger re-fetch"
      pattern: "onPlacementChange|onEventTypeChange"
---

<objective>
Verify the advanced filter drawer visually on mobile and desktop, confirm filter application works end-to-end, and fix any issues found during testing.

Purpose: Ensure the drawer implementation matches the UI spec and provides a good user experience before marking the phase complete.
Output: Verified, working advanced filter drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-advanced-filter-drawer/02-UI-SPEC.md
@.planning/phases/02-advanced-filter-drawer/02-01-SUMMARY.md
@src/components/explore/AdvancedFilterDrawer.tsx
@src/components/explore/ExploreFilters.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test build and fix any issues</name>
  <files>src/components/explore/AdvancedFilterDrawer.tsx, src/components/explore/ExploreFilters.tsx</files>
  <read_first>
    - src/components/explore/AdvancedFilterDrawer.tsx (verify it compiled correctly)
    - src/components/explore/ExploreFilters.tsx (verify integration is correct)
    - .planning/phases/02-advanced-filter-drawer/02-UI-SPEC.md (cross-reference spec compliance)
  </read_first>
  <action>
Run `npx tsc --noEmit && npm run build` and fix any errors.

Then audit both files against 02-UI-SPEC.md for compliance. Check specifically:
1. Count badge uses `bg-accent text-white text-[10px] font-bold min-w-[18px] h-[18px] rounded-full`
2. Mobile sheet has `max-h-[85dvh]`, `overflow-y-auto scrollbar-none`, `rounded-t-2xl`
3. Desktop dropdown has `bg-background/95 backdrop-blur-lg border border-border rounded-xl shadow-lg`
4. "Clear advanced filters" button has `min-h-[44px]` touch target
5. Following toggle has `aria-pressed` attribute
6. X close button has `aria-label="Close advanced filters"`
7. Labels use `text-[11px] font-bold uppercase tracking-wide text-text-tertiary` and switch to `text-accent` when active
8. Footer has `pb-[env(safe-area-inset-bottom,0px)]` on mobile
9. Reduced motion: `useReducedMotion()` imported and animations degrade to opacity-only 100ms

Fix any spec deviations found. If no issues, move on.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - AdvancedFilterDrawer.tsx contains `max-h-[85dvh]`
    - AdvancedFilterDrawer.tsx contains `bg-background/95 backdrop-blur-lg`
    - AdvancedFilterDrawer.tsx contains `min-h-[44px]` (touch targets)
    - AdvancedFilterDrawer.tsx contains `useReducedMotion`
    - AdvancedFilterDrawer.tsx contains `env(safe-area-inset-bottom`
  </acceptance_criteria>
  <done>Build passes, all UI spec items verified present in code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of drawer on mobile and desktop</name>
  <files>src/components/explore/AdvancedFilterDrawer.tsx, src/components/explore/ExploreFilters.tsx</files>
  <action>Present the verification checklist to the user and wait for approval. No code changes unless the user reports issues.</action>
  <what-built>Advanced filter drawer with mobile bottom sheet and desktop dropdown panel. The primary filter bar has been decluttered — placement, event type, and following toggle moved into the drawer. A "More filters" button with count badge provides access.</what-built>
  <how-to-verify>
    1. Visit https://vgc-team-report.vercel.app/explore (or localhost:3000/explore if running locally)
    2. **Primary bar check:** Confirm the filter bar shows ONLY: search category tabs, search input, sort dropdown, species input, regulation dropdown, archetype chips, and "More filters" button. Placement, event type, and following toggle should NOT be on the primary bar.
    3. **Desktop drawer:** Click "More filters" — a dropdown panel should appear below the filter bar with placement, event type, and following toggle. Press Escape to close.
    4. **Filter application:** Open drawer, select "Top 8" placement — results should filter immediately. The "More filters" button should show a badge with "1".
    5. **Badge count:** Set event type too — badge should show "2". Click "Clear advanced filters" — badge disappears.
    6. **Mobile drawer:** Resize browser to < 640px width (or use DevTools mobile viewport). Click "More filters" — a bottom sheet should slide up from the bottom with the same filters.
    7. **Accessibility:** Tab through the "More filters" button — it should have a visible focus ring. Inside the drawer, Tab should navigate between filter controls.
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms primary bar shows only high-frequency filters
    - User confirms drawer opens on both mobile and desktop
    - User confirms filters apply instantly from the drawer
    - User confirms badge count updates correctly
    - User types "approved"
  </acceptance_criteria>
  <done>User has approved the visual implementation of the advanced filter drawer on both mobile and desktop viewports.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Build passes: `npx tsc --noEmit && npm run build`
2. Human verified: drawer works on mobile and desktop
3. Filters apply instantly
4. Badge count reflects active advanced filters
5. Keyboard navigation works (Tab, Escape)
</verification>

<success_criteria>
- User approves the visual implementation
- All filters work correctly when applied from the drawer
- Mobile bottom sheet and desktop dropdown both function properly
- No regressions to existing filter functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-filter-drawer/02-02-SUMMARY.md`
</output>
