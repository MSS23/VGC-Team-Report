---
phase: 01-follow-creators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/user/follow/route.ts
autonomous: true
requirements:
  - "Follows table"
  - "follow/unfollow API (Clerk auth required)"
  - "follow button UI"
  - "follower/following counts on creator pages"

must_haves:
  truths:
    - "Follows table exists in live Neon database with correct schema"
    - "GET /api/user/follow?creator=X returns a single boolean check without fetching all follows"
    - "GET /api/user/follow (no params) still returns full following list"
    - "POST /api/user/follow creates a follow row"
    - "DELETE /api/user/follow removes a follow row"
    - "FollowButton renders on creator profiles for logged-in users"
    - "followerCount displays on creator profile stats row"
  artifacts:
    - path: "src/app/api/user/follow/route.ts"
      provides: "Follow CRUD API with single-creator check"
      exports: ["GET", "POST", "DELETE"]
  key_links:
    - from: "src/components/social/FollowButton.tsx"
      to: "/api/user/follow"
      via: "fetch call"
      pattern: "fetch.*api/user/follow"
    - from: "src/app/api/creator/[name]/route.ts"
      to: "follows table"
      via: "COUNT query"
      pattern: "SELECT COUNT.*FROM follows"
---

<objective>
Verify existing follow system and add efficiency improvement to the follow API.

Purpose: The follow system (DB, API, button, counts) is already implemented. This plan verifies it works end-to-end and adds a targeted `?creator=X` query param to GET /api/user/follow so the FollowButton can check follow status without fetching all followed creators.

Output: Verified follow system with efficient single-creator check endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-follow-creators/01-RESEARCH.md

@src/app/api/user/follow/route.ts
@src/components/social/FollowButton.tsx
@src/components/social/CreatorProfile.tsx
@src/app/api/creator/[name]/route.ts
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify existing follow system and add single-creator check to GET endpoint</name>
  <files>src/app/api/user/follow/route.ts</files>
  <read_first>
    - src/app/api/user/follow/route.ts
    - src/lib/db.ts
    - src/components/social/FollowButton.tsx
    - src/components/social/CreatorProfile.tsx
    - src/app/api/creator/[name]/route.ts
  </read_first>
  <action>
1. Verify the follows table schema exists in `src/lib/db.ts` `ensureTable()` — confirm `CREATE TABLE IF NOT EXISTS follows` with columns `(user_id TEXT NOT NULL, creator_name TEXT NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW())`, PK `(user_id, creator_name)`, and indexes `idx_follows_user` and `idx_follows_creator`.

2. Verify `FollowButton.tsx` is embedded in `CreatorProfile.tsx` and that `followerCount` is rendered in the creator profile stats row.

3. In `src/app/api/user/follow/route.ts`, modify the GET handler to support an optional `?creator=X` query parameter for efficient single-creator follow checks. The current GET handler signature is `export async function GET()` — change it to `export async function GET(request: Request)` to access the URL.

Add this logic AFTER the auth check and BEFORE the existing full-list query:

```typescript
const url = new URL(request.url);
const creatorParam = url.searchParams.get("creator");
if (creatorParam) {
  const row = await sql`SELECT 1 FROM follows WHERE user_id = ${userId} AND LOWER(creator_name) = ${creatorParam.toLowerCase()} LIMIT 1`;
  return NextResponse.json({ following: row.length > 0 });
}
```

The existing full-list behavior (no `creator` param) must remain unchanged — it returns `{ following: string[] }`.

4. Run `npx tsc --noEmit` to confirm no type errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms `request: Request` in the GET function signature in `src/app/api/user/follow/route.ts`
    - grep confirms `url.searchParams.get("creator")` in `src/app/api/user/follow/route.ts`
    - grep confirms `SELECT 1 FROM follows WHERE user_id` in `src/app/api/user/follow/route.ts`
    - grep confirms `LOWER(creator_name)` in the single-creator query
    - grep confirms the original full-list query `SELECT creator_name, created_at FROM follows` is still present
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>GET /api/user/follow?creator=X returns `{ following: boolean }` for a single creator. GET /api/user/follow (no params) still returns `{ following: string[] }`. All existing follow system components verified present and functional. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and run full build gate</name>
  <files></files>
  <read_first>
    - src/app/api/user/follow/route.ts
  </read_first>
  <action>
1. Run `npx tsc --noEmit && npm run build` to verify the full build passes with the updated follow API route.
2. If any errors occur, fix them and re-run until clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Full build passes cleanly with the updated follow API. No regressions introduced.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` passes
- GET /api/user/follow route accepts optional `?creator=X` param
- Original follow API behavior (POST, DELETE, GET full list) unchanged
</verification>

<success_criteria>
- Follow system verified: table schema in db.ts, API routes functional, FollowButton in CreatorProfile, followerCount rendered
- Single-creator check endpoint added: GET /api/user/follow?creator=X returns { following: boolean }
- Full build green
</success_criteria>

<output>
After completion, create `.planning/phases/01-follow-creators/01-01-SUMMARY.md`
</output>
