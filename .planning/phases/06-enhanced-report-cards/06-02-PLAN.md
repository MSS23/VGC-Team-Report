---
phase: 06-enhanced-report-cards
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: [CARD-06]

must_haves:
  truths:
    - "Explore page CLS score is not degraded after card changes"
    - "Explore page median load time is not degraded after card changes"
  artifacts: []
  key_links: []
---

<objective>
Verify that the enhanced report card changes do not degrade explore page performance.

Purpose: CARD-06 requires that CLS and load time do not regress. Since there is no automated web-vitals instrumentation, this requires a manual Lighthouse check on the deployed site.

Output: Performance verified, phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-enhanced-report-cards/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify enhanced cards visually and check performance</name>
  <files>src/components/explore/ReportCard.tsx</files>
  <action>
User visually verifies the enhanced report cards on the live or local site and runs a Lighthouse performance check.

What was built: Enhanced ReportCard with larger sprites, archetype badges below title, regulation pill in corner, creator name styled as link.

How to verify:
1. Visit https://vgcteamreport.com/explore (or local dev at http://localhost:3000/explore)
2. Verify each card shows:
   - Larger Pokemon sprites at the top
   - Archetype badges (Rain, Trick Room, etc.) below the team name
   - Regulation tag visible in the top-right corner without hovering
   - Creator name looks like a link (colored, underlines on hover)
   - Placement badges still show gold/silver/bronze tiering
3. Run Lighthouse on the /explore page (Chrome DevTools > Lighthouse > Performance):
   - CLS should be less than 0.1
   - No significant load time regression
4. Check mobile view (responsive mode, 375px width):
   - Cards stack properly, no overflow
   - Sprites and badges fit within card bounds
  </action>
  <verify>
    <automated>echo "Manual checkpoint — user confirms visual quality and Lighthouse CLS < 0.1"</automated>
  </verify>
  <done>User approves visual appearance and confirms Lighthouse CLS remains below 0.1 with no load time regression</done>
</task>

</tasks>

<verification>
User confirms visual quality and performance metrics via Lighthouse check.
</verification>

<success_criteria>
- User approves visual appearance of enhanced cards
- Lighthouse CLS < 0.1 on /explore
- No load time regression reported
</success_criteria>

<output>
After completion, create `.planning/phases/06-enhanced-report-cards/06-02-SUMMARY.md`
</output>
