---
phase: 06-enhanced-report-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/explore/ReportCard.tsx
autonomous: true
requirements: [CARD-01, CARD-02, CARD-03, CARD-04, CARD-05]

must_haves:
  truths:
    - "Each report card shows 6 Pokemon sprites prominently at the top with consistent sizing"
    - "Each report card shows archetype badges inline below the team name/title"
    - "Tournament placement badge uses tiered gold/silver/bronze visual distinction"
    - "Creator name is a clickable link styled as a link to their profile"
    - "Regulation tag is always visible on every card without hover or expansion"
  artifacts:
    - path: "src/components/explore/ReportCard.tsx"
      provides: "Enhanced report card with prominent sprites, archetype badges, placement tier, creator link, regulation tag"
      contains: "getPlacementStyle"
  key_links:
    - from: "src/components/explore/ReportCard.tsx"
      to: "/creator/{name}"
      via: "onClick handler on creator name span"
      pattern: "creator/"
---

<objective>
Enhance the ReportCard component to make key team information more prominent and scannable at a glance.

Purpose: The existing ReportCard already displays sprites, placement, creator, archetype, and regulation data. This plan restructures the visual layout to make each element MORE prominent — larger sprites, bolder archetype badges, always-visible regulation tag, and creator name styled as a clear link.

Output: Enhanced ReportCard.tsx with improved visual hierarchy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tournament-results-mode/05-02-SUMMARY.md

<interfaces>
<!-- Current ReportCard structure (from src/components/explore/ReportCard.tsx) -->

```typescript
export interface ExploreReport {
  id: string;
  species: string[];
  tournamentName?: string;
  creatorName?: string;
  placement?: string;
  teamSummary?: string;
  createdAt: string;
  updatedAt: string;
  viewCount?: number;
  likeCount?: number;
  reactionCounts?: Record<string, number>;
  commentCount?: number;
  isVerified?: boolean;
  tags?: { archetype?: string[]; regulation?: string; eventType?: string };
  collaborators?: string[];
}
```

Key existing helpers:
- `getPlacementStyle(placement: string): string` — returns Tailwind classes for tiered placement badges (amber=1st, slate=Top4, orange=Top8, default=other)
- `CardSprite({ species })` — renders a lazy-loaded sprite with fallback chain via `getSpriteUrls`
- `relativeTime(date)` — formats timestamps as relative strings
- `getSpriteUrls(species)` — returns array of sprite URL candidates for fallback

Current card layout order: Sprites row -> Title+Placement -> Creator -> Tags -> Social+Timestamp
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enhance ReportCard visual hierarchy for prominent at-a-glance information</name>
  <files>src/components/explore/ReportCard.tsx</files>
  <read_first>
    - src/components/explore/ReportCard.tsx
    - src/components/explore/ExploreContent.tsx
  </read_first>
  <action>
Restructure the ReportCard component layout to make key information more prominent. All changes are CSS/layout — no new data fields, no API changes. The ExploreReport interface stays unchanged.

**Sprites (CARD-01):** Increase sprite prominence.
- Increase CardSprite image size from `w-8 h-8 sm:w-10 sm:h-10` to `w-10 h-10 sm:w-12 sm:h-12` for larger, more scannable icons
- Add a subtle background container behind the sprites row: `bg-surface-alt/30 rounded-lg py-2` within the existing padding wrapper
- Keep lazy loading and fallback behavior unchanged

**Archetype Badges (CARD-02):** Move archetype badges to be more prominent.
- Move archetype badges from the tags section to directly below the title/tournament name line (before creator)
- Increase archetype badge font from `text-[9px]` to `text-[10px]` for better readability
- Keep the existing `bg-accent/10 text-accent` styling with rounded pill shape
- Show up to 3 archetypes (currently 2) before the "+N" overflow

**Placement Badge (CARD-03):** Already well-styled from Phase 05 (getPlacementStyle). No changes needed — the gold/silver/bronze tiered styling with star icons is already in place and meets the requirement.

**Creator Link (CARD-04):** Make the creator name more clearly a link.
- Add `text-accent/80 hover:text-accent` to creator name span (currently just `hover:text-accent`)
- Add a subtle underline on hover: `hover:underline`
- Keep the existing onClick handler that navigates to `/creator/{name}`

**Regulation Tag (CARD-05):** Make regulation always visible and prominent.
- Move the regulation tag out of the conditional tags block into its own always-visible position
- Place it as a small pill in the top-right corner of the card (absolute positioned within the card)
- Style: `absolute top-2 right-2 text-[9px] font-bold px-1.5 py-0.5 rounded bg-blue-500/15 text-blue-600 dark:text-blue-400 z-10`
- The card's outer container needs `relative` added to its className
- Keep regulation in the tags section too if other tags exist, OR remove it from tags to avoid duplication — remove it from the tags section since it now has its own dedicated position

**Tags section cleanup:** After moving archetype badges and regulation out:
- The tags section now only shows eventType and any overflow archetype badges
- If tags section would be empty (no eventType, no overflow archetypes), don't render the flex wrapper

Preserve all existing functionality: motion animation variants, card-hover class, social indicators, timestamp, verified badge, collaborators display. Do not change the ExploreReport interface or any data logic.
  </action>
  <verify>
    <automated>cd "C:/Users/msidh/Documents/Projects/VGC Team Report" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "w-10 h-10 sm:w-12 sm:h-12" src/components/explore/ReportCard.tsx (larger sprites)
    - grep -q "absolute top-2 right-2" src/components/explore/ReportCard.tsx (regulation corner pill)
    - grep -q "hover:underline" src/components/explore/ReportCard.tsx (creator link underline)
    - grep -q "slice(0, 3)" src/components/explore/ReportCard.tsx (3 archetype badges)
  </acceptance_criteria>
  <done>ReportCard displays larger sprites, archetype badges below title, regulation pill in top-right corner, creator name styled as link with underline, placement badges unchanged from Phase 05</done>
</task>

<task type="auto">
  <name>Task 2: Build verification — ensure card changes compile and render correctly</name>
  <files>src/components/explore/ReportCard.tsx</files>
  <read_first>
    - src/components/explore/ReportCard.tsx
  </read_first>
  <action>
Run a full build to verify no regressions from the card layout changes. Check that:
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. Review the final ReportCard.tsx to confirm:
   - ExploreReport interface is unchanged
   - All conditional rendering paths still work (cards with/without placement, with/without creator, with/without tags)
   - Lazy loading attributes still present on sprite images
   - Motion animation variants still present on outer wrapper

If any build errors, fix them in ReportCard.tsx before completing.
  </action>
  <verify>
    <automated>cd "C:/Users/msidh/Documents/Projects/VGC Team Report" && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - npm run build exits 0
    - grep -q "loading=\"lazy\"" src/components/explore/ReportCard.tsx (lazy loading preserved)
    - grep -q "variants=" src/components/explore/ReportCard.tsx (motion variants preserved)
    - grep -q "ExploreReport" src/components/explore/ReportCard.tsx (interface preserved)
  </acceptance_criteria>
  <done>Build passes clean, all existing card functionality preserved, no type errors</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `npm run build` — successful build
3. Visual check: regulation tag visible without hover on every card
4. Visual check: sprites are larger and more prominent
5. Visual check: archetype badges appear below team name
6. Visual check: creator name looks like a clickable link
</verification>

<success_criteria>
- ReportCard shows larger Pokemon sprites (w-10/w-12 vs w-8/w-10)
- Archetype badges displayed below title, up to 3 shown
- Regulation tag always visible in top-right corner pill
- Creator name styled as link with hover underline
- Placement badges retain Phase 05 gold/silver/bronze tiering
- Build passes with zero errors
- No changes to ExploreReport interface or API
</success_criteria>

<output>
After completion, create `.planning/phases/06-enhanced-report-cards/06-01-SUMMARY.md`
</output>
