---
phase: 05-tournament-results-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/explore/ReportCard.tsx
autonomous: true
requirements: [TOUR-05]

must_haves:
  truths:
    - "Report cards display placement prominently with visual distinction for Top 4 and Top 8"
    - "1st place gets gold highlight treatment"
    - "Top 4 gets silver/distinguished highlight"
    - "Top 8 gets bronze/subtle highlight"
    - "Top 16 and other placements remain visible but without special highlight"
    - "Cards without placement show no placement badge (no regression)"
  artifacts:
    - path: "src/components/explore/ReportCard.tsx"
      provides: "Enhanced placement badge with tiered visual distinction"
      contains: "placementStyle\|placementClass\|getPlacement"
  key_links:
    - from: "src/components/explore/ReportCard.tsx"
      to: "report.placement"
      via: "conditional styling based on placement value"
      pattern: "Top 4|Top 8|1st"
---

<objective>
Enhance the ReportCard placement badge with tiered visual distinction. Top placements (1st, Top 4, Top 8) get prominent color-coded badges that stand out, making tournament results scannable at a glance.

Purpose: TOUR-05 — placement displayed prominently on report cards with visual distinction for high placements.
Output: Enhanced placement badge styling in ReportCard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current ReportCard placement badge (line 84-88 of ReportCard.tsx) -->
```typescript
{report.placement && (
  <span className="flex-shrink-0 inline-flex items-center px-1.5 sm:px-2 py-0.5 text-[9px] sm:text-[10px] font-extrabold rounded-md tracking-wide bg-accent-surface text-accent">
    {report.placement}
  </span>
)}
```

<!-- ExploreReport interface -->
```typescript
export interface ExploreReport {
  id: string;
  species: string[];
  tournamentName?: string;
  creatorName?: string;
  placement?: string;  // e.g. "1st", "Top 4", "Top 8", "Top 16"
  // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tiered placement badge styling to ReportCard</name>
  <files>src/components/explore/ReportCard.tsx</files>
  <read_first>
    - src/components/explore/ReportCard.tsx (full file — current placement badge at line 84-88, full component structure)
  </read_first>
  <action>
    1. Add a helper function above the ReportCard component (inside the file, not exported):

    ```typescript
    function getPlacementStyle(placement: string): string {
      if (placement === "1st") {
        // Gold — champion treatment
        return "bg-amber-500/20 text-amber-600 dark:text-amber-400 ring-1 ring-amber-500/30";
      }
      if (placement === "Top 4") {
        // Silver — finalist treatment
        return "bg-slate-400/15 text-slate-600 dark:text-slate-300 ring-1 ring-slate-400/30";
      }
      if (placement === "Top 8") {
        // Bronze — strong showing
        return "bg-orange-500/15 text-orange-600 dark:text-orange-400 ring-1 ring-orange-500/25";
      }
      // Default — standard badge for Top 16 and other placements
      return "bg-accent-surface text-accent";
    }
    ```

    2. Replace the existing placement badge (lines 84-88) with an enhanced version:

    ```tsx
    {report.placement && (
      <span className={`flex-shrink-0 inline-flex items-center gap-1 px-1.5 sm:px-2 py-0.5 text-[9px] sm:text-[10px] font-extrabold rounded-md tracking-wide ${getPlacementStyle(report.placement)}`}>
        {(report.placement === "1st" || report.placement === "Top 4") && (
          <svg width="10" height="10" viewBox="0 0 24 24" fill="currentColor" stroke="none" className="flex-shrink-0">
            <path d="M12 2l2.4 7.4h7.6l-6 4.6 2.3 7.4L12 16.8l-6.3 4.6 2.3-7.4-6-4.6h7.6z" />
          </svg>
        )}
        {report.placement}
      </span>
    )}
    ```

    The star icon appears only for 1st place and Top 4, adding a small visual flourish that draws the eye.

    3. Keep the existing layout structure (flex items-start justify-between) — just update the badge classes and add the icon.

    4. Do NOT change the ExploreReport interface or any other part of ReportCard — this is purely a styling enhancement to the existing placement badge.
  </action>
  <verify>
    <automated>cd "C:/Users/msidh/Documents/Projects/VGC Team Report" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getPlacementStyle" src/components/explore/ReportCard.tsx
    - grep -q "amber" src/components/explore/ReportCard.tsx
    - grep -q "slate" src/components/explore/ReportCard.tsx
    - grep -q "orange" src/components/explore/ReportCard.tsx
    - grep -q "1st" src/components/explore/ReportCard.tsx
    - grep -q "Top 4" src/components/explore/ReportCard.tsx
    - grep -q "Top 8" src/components/explore/ReportCard.tsx
  </acceptance_criteria>
  <done>Placement badge shows gold (1st), silver (Top 4), bronze (Top 8) color tiers with ring highlights. 1st and Top 4 include a star icon. Top 16 and other placements use the existing accent style. Cards without placement show no badge (no regression). tsc clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. Open /explore — cards with "1st" placement show gold/amber badge with star
3. Cards with "Top 4" show silver/slate badge with star
4. Cards with "Top 8" show bronze/orange badge
5. Cards with "Top 16" or other placement show standard accent badge
6. Cards without placement show no badge at all
7. Badge text remains readable in both light and dark mode
</verification>

<success_criteria>
- Placement badges have 4 visual tiers: gold (1st), silver (Top 4), bronze (Top 8), standard (other)
- Star icon on 1st and Top 4 badges
- Readable in light and dark mode
- No regression for cards without placement
- No type errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-tournament-results-mode/05-02-SUMMARY.md`
</output>
