---
phase: 03-shareable-filter-urls
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/explore/ExploreFilters.tsx
autonomous: false
requirements: [URL-04]

must_haves:
  truths:
    - "User sees a Copy link button near the filter bar when any filter is active"
    - "Clicking Copy link copies the current browser URL to clipboard"
    - "Copy link button is hidden when no filters are active (default state)"
    - "User gets visual feedback (tooltip or text change) confirming the link was copied"
  artifacts:
    - path: "src/components/explore/ExploreFilters.tsx"
      provides: "Copy link button integrated into filter bar"
      contains: "Copy link"
  key_links:
    - from: "src/components/explore/ExploreFilters.tsx"
      to: "navigator.clipboard.writeText"
      via: "onClick handler on Copy link button"
      pattern: "clipboard.writeText"
---

<objective>
Add a "Copy link" button to ExploreFilters that appears when any filter is active, copies the current URL to clipboard, and shows feedback. Then visually verify the full shareable URL flow.

Purpose: This completes the user-facing sharing feature. Without the button, users would have to manually copy from the address bar.
Output: Copy link button in ExploreFilters + visual verification of the entire flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shareable-filter-urls/03-01-SUMMARY.md
@src/components/explore/ExploreFilters.tsx
</context>

<interfaces>
<!-- ExploreFilters already receives all filter values as props -->

From src/components/explore/ExploreFilters.tsx:
```typescript
interface ExploreFiltersProps {
  query: string;
  sort: "newest" | "updated" | "popular" | "views";
  searchCategory: SearchCategory;
  regulation: string;
  eventType: string;
  archetype: string;
  species: string;
  placement: string;
  followingOnly: boolean;
  // ... plus onChange handlers
}
```

The button needs to detect "any filter active" by checking if ANY prop differs from its default:
- query !== ""
- sort !== "newest"
- searchCategory !== "all"
- regulation !== ""
- eventType !== ""
- archetype !== ""
- species !== ""
- placement !== ""
- followingOnly !== false
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Copy link button to ExploreFilters</name>
  <files>src/components/explore/ExploreFilters.tsx</files>
  <read_first>
    - src/components/explore/ExploreFilters.tsx (current layout to find insertion point)
    - src/hooks/useExploreUrlSync.ts (confirm URL param keys for hasActiveFilters logic)
  </read_first>
  <action>
    Add a "Copy link" button to ExploreFilters that:

    1. **Visibility:** Only renders when at least one filter differs from its default. Compute `hasActiveFilters` from props:
       ```
       const hasActiveFilters = query !== "" || sort !== "newest" || searchCategory !== "all" || regulation !== "" || eventType !== "" || archetype !== "" || species !== "" || placement !== "" || followingOnly;
       ```

    2. **Position:** Place the button in the search category tabs row (the first `div` with `flex items-center gap-1.5`), after the "More filters" button. Use `ml-auto` to push it to the right end of the row.

    3. **Appearance:** Use a link/share icon (SVG inline, 12x12). Text: "Copy link". Style: same `text-xs font-bold px-3.5 py-2 rounded-lg` as the category tabs but with accent-colored styling when visible. Use `bg-accent/10 text-accent hover:bg-accent/20` for a subtle but distinct look.

    4. **Click handler:** On click:
       - Call `navigator.clipboard.writeText(window.location.href)`
       - Set local state `copied` to true
       - After 2 seconds, reset `copied` to false
       - While `copied` is true, change button text to "Copied!" and swap icon to a checkmark

    5. **Animation:** Wrap in AnimatePresence (already imported) with a simple fade in/out (`initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}`).

    6. **Accessibility:** Add `aria-label="Copy shareable link to clipboard"` on the button.

    7. **No changes to ExploreFiltersProps** — the button only reads existing props, no new props needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep "Copy link" src/components/explore/ExploreFilters.tsx confirms button text exists
    - grep "clipboard.writeText" src/components/explore/ExploreFilters.tsx confirms clipboard API usage
    - grep "hasActiveFilters" src/components/explore/ExploreFilters.tsx confirms conditional rendering
    - grep "Copied" src/components/explore/ExploreFilters.tsx confirms feedback state
    - grep "aria-label" src/components/explore/ExploreFilters.tsx includes the copy button aria-label
  </acceptance_criteria>
  <done>Copy link button appears in filter bar when any filter is active, copies current URL to clipboard on click, shows "Copied!" feedback for 2s, hidden when no filters active</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of shareable URL flow</name>
  <files>none — visual verification only</files>
  <action>
    Present the verification checklist to the user. Start the dev server if not running (`npm run dev`). No code changes in this task — purely user verification of the complete shareable URL feature built across Plans 01 and 02.
  </action>
  <what-built>
    Complete shareable filter URL system:
    - All filter changes sync to browser URL in real-time
    - Copy link button appears when filters are active
    - Shared URLs restore exact filter state
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/explore
    2. Verify the URL is clean: `/explore` with no params
    3. Verify NO "Copy link" button is visible (no active filters)
    4. Select regulation "Reg H" — URL should update to include `?regulation=Reg+H`
    5. Verify "Copy link" button now appears in the filter bar
    6. Add a species filter and archetype — URL should include all params
    7. Click "Copy link" — button should briefly say "Copied!"
    8. Open a new tab, paste the URL — all filters should be pre-populated
    9. Verify the results in both tabs match
    10. Clear all filters — URL returns to `/explore`, Copy link button disappears
    11. Test on mobile viewport (resize browser to ~375px) — button should still be accessible
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - User confirms filters sync to URL
    - User confirms Copy link button appears/disappears correctly
    - User confirms shared URL restores filters in new tab
  </acceptance_criteria>
  <done>User has approved the complete shareable filter URL flow</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. Copy link button is conditionally visible based on active filters
4. Full round-trip: set filters -> copy link -> open in new tab -> filters restored
5. Accessible: button has aria-label, keyboard-focusable
</verification>

<success_criteria>
- Copy link button appears only when filters are active
- Clicking copies current URL to clipboard with visual feedback
- Full sharing flow works end-to-end (set filters, copy, paste in new tab, filters restored)
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-shareable-filter-urls/03-02-SUMMARY.md`
</output>
